<%= form_for(@user_video) do |f| %>
  <% if @user_video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_video.errors.count, "error") %> prohibited this user_video from being saved:</h2>

      <ul>
      <% @user_video.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
